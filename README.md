# Portfolio (Dummy)

Simple one-page HTML portfolio, automatisch deployt auf GitHub Pages via GitHub Actions.

- `index.html` – die Seite
- `.github/workflows/deploy.yml` – baut und deployt bei jedem Push auf `main`

Nach einem Push auf `main` läuft der Workflow und die Seite ist unter der GitHub-Pages-URL erreichbar.
